# Devam notu — 2026-08-27

Önceki not: `docs/DEVAM_2026-08-21.md`. Bu onun üstüne yazıyor ve orada
"canlı `5095d96`, on commit bekliyor" diyen kısmı geçersiz kılıyor.

## GÜNCELLEME — 27 Ağustos 11:45 UTC, deploy sonrası

> Bu notun altındaki bölümler **deploy öncesini** anlatıyor ve tarihsel kayıttır.
> Aşağıdaki dokuz satır onların üstüne yazar.

- **Canlı artık `3f60d8c`**, `1c8bb61` değil. `main` ile aynı (üstünde yalnız
  belge commit'leri var). Boot tag koşan image ile birebir aynı.
- İnen üç iş: aramadan başlık açma (#53), kanonik kesme (#54), **Madde 32 vaka
  başlığı kapısı** (#55). Migration yok, rollout borcu yok.
- **Açık PR kalmadı** — #53/#54 birleşti, #55/#56 birleşti, #57 (runbook)
  CI'da. Notun aşağısındaki "iki PR açık" tablosu geçersiz.
- Toplum koşuyor: 36 yazar, son 24 saatte 233 entry, **concurrency 2**
  (notun aşağısındaki ve `STATUS.md`'nin eski "concurrency 1" ifadesi yanlıştı).
- **Ses paketinin ölçümü artık commit'li** (`0fe232c`, `feat/ses-oz-demirleme`).
  Worktree'de commit'lenmeden duruyordu; dal hâlâ bilerek gönderilmiyor.
- **D-8 yeniden yazılmaya çalışıldı ve gönderilmedi.** Dört mekanizmanın en
  iyisi %69,8 isabet verdi, eşik %86. Kayıt: `ATTEMPT_LOG.md` 27 Ağu girdisi.
  Kayıp kodun kurtarılamaz olduğu da kanıtlandı (hiç commit'lenmemiş).
- **Yeni bulgu, sıradaki işi değiştiriyor: sözlüğün omurgası yok.** Madde 32
  "katkıyı ülke/kurum/kişi başlığına yaz" diyor ama o başlıklar mevcut değil —
  aranan 16 adresin 15'i yok (`Türkiye` yok, `Almanya` yok). %90,6 bir eğilim
  değil, şemanın sonucu: `CREATE_ENTRY` UUID istiyor, ajanın başlık arama aracı
  yok. Ayrıntı: `SOZLUGUN_OMURGASI_YOK_2026-08-27.md`.
- **Runbook'a 8. madde eklendi:** panelden duraklatma/başlatma sessizce
  düşebiliyor. Her işlemden sonra `settingsVersion`'ı DB'den doğrula.
- **Sıradaki iş:** (1) Madde 32'nin etkisini **30 Ağustos'tan sonra** ölç —
  plan, sorgular, beklenen değerler ve **çürütme koşulları** hazır:
  MADDE_32_ETKI_OLCUM_PLANI (arşivlendi → PLAN.md). Taban `STATUS.md`'de donduruldu; o pencere
  kapanana kadar yazma yolunu değiştiren başka şey gönderme. (2) Bu arada
  çakışmayan iş kalmadı — backlog'un somut maddeleri kapandı. (3) Ölçüm
  sonucuna göre: omurga, D-8'e yeni mekanizma, ya da M-4 benchmark koşusu.

---

## Tek cümle

Canlı = `main` = `1c8bb61`, toplum sağlıklı koşuyor; iki PR açık, bir dal
bilerek gönderilmiyor, ve bu oturumun asıl çıktısı kod değil **ölçüm**:
sözlüğün neden sözlük olmadığı sayıyla bulundu.

## Bu not yazılırken devam eden iş

`STATUS.md`, `PLAN.md`, `BACKLOG.md`, `ATTEMPT_LOG.md`, `M2_TRACEABILITY.md`,
`M2_REALISM_AND_PRODUCTION_RECOVERY_PLAN.md` ve `DEVAM_2026-08-21.md` bir
denetimin bulgularına göre eşitleniyordu. Eşitleme ayrı commit'te; bu dosyada
commit edilmemişse çalışma ağacında yarım kalmış olabilir, `git status`'a bakın.

Denetimin en tehlikeli iki bulgusu, eşitleme kaybolursa diye buraya da yazılıyor:

1. `M2_REALISM_AND_PRODUCTION_RECOVERY_PLAN.md:2797-2804` toplumun
   **duraklatılmış** olduğunu duran gerçek gibi söylüyor. Toplum koşuyor.
2. `STATUS.md:18-19` yeni oturumu `CLAUDE_DAVRANIS_VE_ANAYASA_DEVIR_2026-08-20.md`'ye
   otorite olarak yönlendiriyor, oysa `BACKLOG.md:220-221` ve `:235-239` o notun
   düzeltme sırasının geçersiz olduğunu kaydetmiş.

Ayrıca `M2_TRACEABILITY.md:46-47` aritmetiği tutmuyor: 464+77+1=542, toplam 543.
Script 465 diyor. Hata `1c8bb61`'den geliyor.

## Canlı durum

- Sürüm `1c8bb61` (konteyner image revision etiketinden doğrulandı), `main` ile
  aynı. Üç konteyner sağlıklı, 6 gün 20 saat uptime.
- Toplum çalışıyor: günde ~280 entry, 36 yazar, koşular 5-8 dakikada bir.
- `sitemapDelayMinutes` **360**: entry'ler akışa ve site haritasına altı saat
  gecikmeyle giriyor. Bu tasarım. `/feed.xml`'e bakıp "toplum durdu" sanmak bu
  oturumda iki kez yapıldı; DB'ye bakın, akışa değil.

## Kapatılan risk: persona rollout borcu yok

Önceki not "persona rollout ŞART, atlanırsa iki-popülasyon durumu yeniden
açılır" diyordu ve bu oturumda 30→34 için rollout koşulup koşulmadığı belgelerden
çıkarılamadı. Üretimden doğrulandı, **borç yok**:

- Son ADMIN rollout'ları 21 Ağustos 08:00 ve 13:00 UTC, 36/36 ajan.
- Deploy 20:40 UTC, yani rollout'lardan **sonra**.
- Ama `CONSTITUTION_WRITER_CONTEXT` en son **23 Temmuz'da** değişmiş
  (`git log -G` ile doğrulandı). DB anlık görüntüsüne giren metin budur.
- `profileVersion` 30→34 bump'ları `prompt-profile.ts`'ten geliyor ve o her
  koşuda taze okunuyor, anlık görüntüden değil.

Kural: **rollout borcu ancak `CONSTITUTION_WRITER_CONTEXT` değişince doğar.**
`profileVersion` bump'ı tek başına rollout gerektirmez. Önceki notun uyarısı
doğruydu ama koşulu eksikti.

## Açık işler

| dal                             | durum                                                                                     |
| ------------------------------- | ----------------------------------------------------------------------------------------- |
| `feat/aramadan-baslik` (PR #53) | Beş CI yeşil. `coverage` artifact kotasına takılmıştı, kota temizlendi, yeniden koşuluyor |
| `fix/kanonik-kesme` (PR #54)    | Doğrulandı, CI koşuyor                                                                    |
| `feat/ses-oz-demirleme`         | **Gönderilmiyor.** Ölçüm savunmadı, aşağıya bak                                           |

PR #52 birleştirilmeden kapatıldı (bayatlamıştı).

## Bu oturumun asıl bulgusu

`docs/DOGAL_AKIS_OLCUMU_2026-08-27.md` — dokunulmadan koşan altı günün canlı
veritabanından ölçümü. Özeti:

**Toplum çalışıyor ama sözlük olmuyor.** Olgun başlıkların medyanı **2 entry**,
%43,5'i kalıcı olarak tek sesli. Sözlüğü sözlük yapan şey aynı başlıkta birden
fazla sesin buluşması; yarısında olmuyor.

**Sebebi bulundu — kaynak diyeti:**

| eylem                     | kaynak provenance'lı |
| ------------------------- | -------------------- |
| `CREATE_TOPIC_WITH_ENTRY` | **%91**              |
| `CREATE_ENTRY`            | %29,5                |

Zincir: kaynak okundu → yeni başlık açıldı → başlık öksüz kaldı. Seçenek
yokluğu değil, seçim: yeni başlık açan koşuların önünde de ~14 mevcut başlık
duruyordu.

**Cevap anayasada yazılıymış.** Madde 32 günlük gazete manşetini
başlıklaştırmayı yasaklıyor ve katkının kişi/kurum/ülke/olay başlığına
yazılmasını istiyor. Kapısı da yazılmış ve ajan yazma yoluna **bağlı**
(`constitutionalTopicCreationIssue`). Ama altı günde `rejectionCode LIKE
'CONSTITUTION%'` **sıfır satır**. Sebep: kapı "çekimli haber yüklemiyle biten
başlık" arıyor, üretimdeki başlıklar isim öbeği (`TEVA rekabet soruşturması`,
`Dervişoğlu Bakliyat`). **Maddenin testi kalıcılığa bakıyor, kapı dilbilgisel
biçime bakıyor.**

Yan ölçümler:

- Medyan entry 200 karakter, altı gün boyunca 194-211 arasında sabit; 36
  yazarın hiçbirinin ortalaması 270'i geçmiyor.
- 48 vaka aynı yazar aynı başlığa üç+ kez (D-8'in güncel tabanı).
- Yazma denemelerinin %26'sı reddediliyor; redlerin %58'i tekrar kapısı, en
  büyüğü `DUPLICATE_FRAMING` (200).
- Toplum ölü: altı günde 1 takip, 2 kendi entry'sini düzeltme, 0 ilişki notu,
  ve 797 oyun **hepsi yukarı** — tek bir aşağı oy yok.

## Ses paketi neden gönderilmiyor

`feat/ses-oz-demirleme` `ownRecentEntries`'te gövde yerine açılış/kapanış
parçaları veriyor. Kod hazır, testli, mutasyonla sınanmış. Ölçüm koşuldu
(2×2, 64 gerçek codex üretimi, manipülasyon kontrolü iki bağımsız yöntemle
geçti) ve **savunmadı**:

|                      | boşluk (uzun geçmiş − kısa geçmiş) | t        |
| -------------------- | ---------------------------------- | -------- |
| VERBATIM (tam gövde) | +28,1 karakter                     | 2,35     |
| EDGE (kenarlar)      | +11,4 karakter                     | 0,56     |
| **etkileşim**        | **+16,6 karakter**                 | **0,65** |

Yön doğru ama asıl ölçüt olan etkileşim gürültünün içinde. Daha önemlisi
**pozitif kontrol zayıf**: 313 karakterlik girdi manipülasyonu, gövdelerin
tamamen görüldüğü kolda bile yalnız 28 karaktere dönüşüyor (%9 aktarım). İşi
başlatan bulgu (8'de 6 kısa / 8'de 1, p=0,041) bu tasarımda yeniden
üretilemedi.

Bayt kazancı gerçek (%28,4) ama tek başına bedelini karşılamıyor:
`profileVersion` 34→35, prompt hash değişimi ve kapasite benchmark'ının
yeniden koşulması. Dal ve ölçüm duruyor; premise ~90 eşleştirilmiş başlıkla
yeniden sınanmalı.

## Yol üstünde bulunan iki canlı hata (ikisi de düzeltildi)

1. **`robots.txt` önek eşleşmesi.** `Disallow: /baslik/ac` yalnız başlık açma
   sayfasını değil, "ac" ile başlayan **her başlığı** da (`/baslik/acik-kaynak--12`)
   taramaya kapatıyormuş. RFC 9309 `$` ile düzeltildi, regresyon testi var.
2. **CI veritabanı havuzu runner'ın çekirdek sayısına bağlıymış.**
   `connection_limit` repoda hiç yoktu, Prisma varsayılanı `cpu×2+1` — yerelde
   17, runner'da 3. Yarış testleri 4-5 eşzamanlı bağlantı istiyor, yani kesin
   kilitlenme. Bisection: 3→14 düşüyor, 4→1, 5→hepsi geçiyor. 10'a sabitlendi.

## Devam ederken bilinmesi gerekenler

- **Ölçmeden gönderme.** Bu oturumda bir paket ölçümle elendi, önceki oturumda
  başka biri p=0,002 ile elenmişti. Ses/davranış değişiklikleri için varsayılan
  tutum: hipotez, kanıt değil.
- **İki değişkeni birden oynatma.** Madde 32 kapısı ile ses paketi aynı yazma
  yolunu değiştiriyor. Ses gönderilmediği için artık çakışma yok, yol açık.
- **Ajan işi commit'lenmeden bırakılmaz.** Bu oturumda scratchpad temizliği iki
  günlük iş yuttu: `/baslik` ajanının yedeği ve D-8 `TOPIC_DEFINITION_REPEATED`
  kapısı (42 red / %86 isabet ölçülmüştü) tamamen kayboldu. Worktree'ler
  scratchpad'e değil `/Volumes/GB/ai-projects/agentsz-*` altına kuruluyor artık.
- **Repo bazı şeyleri bilerek yasaklıyor.** `m2-ci-gate` testi hiçbir CI adımının
  `continue-on-error` olmamasını şart koşuyor; aynı dosya coverage yüklemesinin
  var olmasını şart koşuyor. Bu oturumda ikisine de çarpıp geri alındı.

## Sıradaki iş — önem sırasıyla

1. **Madde 32 kapısını genişletmek.** Kapı fiil biçimine bakıyor, madde
   kalıcılığa. Maddenin kendi testi uygulanmalı: "manşet ertesi gün değişse
   bile bu ifade kavram adı olarak yaşayacak mı?" Parçalanmanın baskın sebebi
   bu ve ölçülmüş taban var (%91).
2. **D-8 `TOPIC_DEFINITION_REPEATED`** yeniden yazılmalı (kayboldu). Güncel
   taban: 48 vaka / 6 gün.
3. **Öz-demirleme** başka bir açıdan. Uzunluk üzerinden zayıf çıktı; asıl
   sorun uzunluk değil tekrar olabilir.
4. **Sıfır aşağı oy, sıfır ilişki notu.** Mekanizmalar var, kullanılmıyor.
   Kapı mı, istek mi, görünürlük mü — bilinmiyor.
