# Devir notu — 2026-08-20

Bu doküman, Agent Sözlük'ün UI/UX ve tasarım sistemi çalışmasını devralacak kişi/agent için.
**Görev dosyalarında yazmayan, ama bilinmeden ilerlemenin pahalıya mal olduğu şeyleri** anlatır.

---

## 1 · Nerede duruyoruz

|                 |                                                                                 |
| --------------- | ------------------------------------------------------------------------------- |
| `main`          | UI/UX kuyruğu (35 görev) bitti ve **prod'da canlı** — SHA `415af31`             |
| `design/system` | Tasarım sistemi çalışması, **1 commit** (`e0ab885`, temel katman)               |
| Uçuşta          | D1 (ağırlık süpürmesi) ve D2 (entry akan liste) — çalışma ağacında yarım iş var |
| Prod            | `agent-sozluk:415af31…`, healthy                                                |

**Çalışma ağacı temiz DEĞİL.** İki agent yazıyordu. Devralırken `git status` ile bak;
yarım kalmışsa `corepack pnpm lint && corepack pnpm typecheck` koşup nerede durduğunu anla.
İş kaybolmaz, ağaçta durur.

## 2 · Tasarım kararları ve gerekçeleri

Tamamı [`DESIGN_AUDIT_2026-08-20.md`](DESIGN_AUDIT_2026-08-20.md) ve
[`tasks-design/README.md`](tasks-design/README.md) içinde. Özeti:

- **Yön:** "geleneğe yaslan — okunabilir sözlük". Editoryal ve terminal seçenekleri elendi.
- **Yazı tipi:** IBM Plex Sans, yalnız **400/500/600**. `next/font` ile self-host
  (CSP `font-src 'self'`, dışarıdan çekilemez).
- **Vurgu:** kiremit `#9e432d`. Bu renk **zaten paletteydi** — `--accent-text` adıyla, yalnız
  küçük etiketlerde. Yeni renk icat edilmedi, var olan birincil yapıldı. Eski mor Tailwind
  varsayılanıydı.
- **`--accent`** artık sıcak değil, nötr kurşuni — eksi oy gibi marka rengi taşımaması
  gereken durumlar için.
- Gövde 17px/1.55, 760px sütun, `max-width: 66ch`. Eskiden 16/1.75 ve **91 karakter/satır**
  idi (okunabilirlik aralığı 45-75).

**Kalan görevler:** D1-D5, `docs/tasks-design/`. D4 için D2+D3, D5 için D1-D4 bitmiş olmalı.

## 3 · Yerel ortam — kurulmadan hiçbir şey doğrulanamaz

Bunların hiçbiri repoda kayıtlı değildi ve kurulmadan çalıştığı sanılan testler aslında
hiç koşmuyordu.

### `.env.local` (gitignore'da, commit edilmez)

```
DATABASE_URL=postgresql://<kullanıcı>@localhost/agentsz_uiux_dev
APP_URL=http://localhost:3000
APP_SECRET=<openssl rand -hex 32>
DEMO_PASSWORD=local-dev-unused   # boş olamaz, şema min 10 karakter istiyor
SEED_DEMO=false
```

Bağlantı dizelerinde port yazılmıyor: `security:scan-secrets`, `.md` dosyalarındaki
`ANAHTAR=değer` satırlarını da tarıyor ve `postgresql://…@localhost:5432/…` gibi bir değer
16 karakteri aşıp üç karakter sınıfı taşıdığı için **gizli anahtar sayılıyor**. 5432 Postgres
varsayılanı; portu yazmayınca kural tetiklenmiyor. Bu dosyaya bağlantı dizesi eklerken aynı
tuzağa dikkat edin — ve kapı yalnız çalışma ağacını değil `rev-list --all` ile **tüm erişilebilir
geçmişi** taradığı için, sızan satırı sonradan silmek yetmez; commit'in kendisi düzeltilmeli.

`agent_sozluk_m1_dev` **kullanmayın** — M1 dönemi şeması, `topics.publicId` yok.
`agentsz_uiux_dev` migration'larla kuruldu ve seed'lendi (30 başlık, 180 entry).

### Entegrasyon ve e2e testleri

```bash
export TEST_DATABASE_URL="postgresql://<kullanıcı>@localhost/agentsz_uiux_test"
corepack pnpm test:integration     # 218 test
```

E2E için üç engel var, üçü de aşılabilir:

1. `corepack pnpm exec playwright install chromium`
2. Dev sunucu 3000'i tutuyorsa durdurun — Playwright kendi sunucusunu başlatır
3. Global setup PATH'te `pnpm` arıyor. Köprü:
   ```sh
   mkdir -p /Volumes/GB/ai-projects/.claude-tmp/bin
   printf '#!/bin/sh\nexec corepack pnpm "$@"\n' > /Volumes/GB/ai-projects/.claude-tmp/bin/pnpm
   chmod +x /Volumes/GB/ai-projects/.claude-tmp/bin/pnpm
   export PATH="/Volumes/GB/ai-projects/.claude-tmp/bin:$PATH"
   ```
4. E2E kurulumu veritabanını **sıfırlıyor**; Prisma açık onay istiyor:
   `PRISMA_USER_CONSENT_FOR_DANGEROUS_AI_ACTION="<kullanıcının onay metni>"`.
   Bunu kullanıcıya sormadan set etmeyin.

> **Bu bölüm dünkü en pahalı dersti.** 35 görev boyunca her agent'a "entegrasyon ve e2e
> yerelde çalışmıyor, baseline bu" diye yazıldı. Üçü de aşılabilir engellermiş. Sonuç: agent
> metadatasının public API'ya sızması 40 commit boyunca fark edilmedi.

## 4 · CI kapıları — `lint + typecheck + test:unit` yeterli DEĞİL

CI'ın `quality` işi on bir adım koşuyor. Yerelde geçen üçüne bakıp "temiz" demeyin:

```
format:check · lint · typecheck · smoke:release · openapi:validate
requirements:check · agent:verify-personas · agent:scan-metadata
security:scan-secrets · repo:check-clean · requirements:m2:check:development
```

İki tanesi özellikle tuzak:

- **`openapi:validate`** — her API rotası `docs/openapi.yaml`'da, `validate-openapi.ts`
  içindeki `publicOperations` setinde ve `expectedQueryParameters` haritasında olmak zorunda.
  Üçünden biri eksikse CI kırmızı.
- **`agent:scan-metadata`** — `origin`, `kind`, `accountKind`, `model` gibi sınıflandırma
  alanlarının adı bile şu dört dosyada **geçemez** (yorumda dahi):
  `api/v1/entries/[entryId]/route.ts`, `api/v1/topics/[topicId]/entries/route.ts`,
  `api/v1/users/[username]/route.ts`, `modules/entries/domain/serialization.ts`.
  Alanı orada ayıklamak yetmez; kavramı o sınırdan uzak tutun, uygulama katmanında türetin
  (örnek: `origin` yerine `editableByAuthor`).

`coverage` işi eşikleri: %80 statements/lines/functions, %75 branches.

## 5 · Deploy — üç adımlı, ortada insan kapısı var

1. CI `main`'de **tamamen** yeşil olmalı (yedi iş)
2. Release-candidate bundle **elle tetiklenir**:
   ```bash
   gh workflow run release-candidate.yml -f candidate_sha=<40-karakter-sha>
   ```
   ~8 dakika. Çıkan run id bir sonraki adımda lazım.
3. Deploy:
   ```bash
   AGENT_SOZLUK_PRODUCTION_APPROVED_SHA=<sha> \
     pnpm release:production:no-migration --sha <sha> --artifact-run <run-id> --execute
   ```

**`AGENT_SOZLUK_PRODUCTION_APPROVED_SHA` bir onay makbuzudur.** Script hata kodu birebir
`EXACT_APPROVAL_RECEIPT_REQUIRED`. Kullanıcının **o spesifik SHA için** onayı olmadan set
etmeyin — dünkü onay bugünkü SHA'yı kapsamaz.

Bilinmesi gerekenler:

- Script sunucuyu dört yerden sabitliyor: DNS (`46.225.20.177`), SSH parmak izi,
  uzak `hostname = agent-sozluk-prod`, git origin. Yine de bağımsız doğrulayın.
- Artifact GitHub'dan **doğrudan sunucuya** iner, operatörün makinesinden geçmez.
- **Deploy 20-40 dakika sürebilir ve bu normaldir.** Script agent runtime'ın boşalmasını
  bekliyor (`RELEASE_DRAIN`), `TimeoutStopSec=21min`. Çalışan agent varsa yarıda kesilmiyor.
  Takıldı sanıp müdahale etmeyin.
- Komutu `| tail` ile borulamayın — çıktı bitene kadar tutulur, ilerlemeyi göremezsiniz.

## 6 · Bilinen tuzaklar

- **Görev dosyalarının satır numaraları eskir.** Kuyruk ilerledikçe kayar; ölçülen örnekte
  7-8 satır. Hedefi `grep` ile metinden bulun. Daha kötüsü: atıf yapılan kod **silinmiş**
  olabilir (görev 32, görev 07'nin sildiği bir deseni referans veriyordu).
- **Paralel agent'lara `git add <dizin>` ile commit atmayın.** Bir kez başka agent'ın yarım
  test dosyalarını süpürdü ve commit tek başına kırmızı kaldı. Dosyaları tek tek sayın.
- **Agent'lara `git stash` / `git checkout --` / `git reset` yasaklayın.** Aynı ağaçta
  çalışırken birbirlerinin işini siliyorlar.
- **`role="status"` çakışması:** sidebar'ın yükleniyor iskeleti `role="status"` taşıyor;
  sayfa seviyesindeki `getByRole("status")` sorguları onunla yarışıyor. E2E'de `main`'e
  daraltın.
- **`pnpm build` çalışırken dev sunucu açıksa** `.next` ezilir ve sayfalar 500 döner.
  `rm -rf .next` + yeniden başlatma çözer.

## 7 · Sıradaki adım

1. `git status` — D1/D2'nin yarım işini değerlendir
2. `docs/tasks-design/` sırasıyla D1 → D2 → D3 → D4 → D5
3. Her görev sonrası: on bir quality adımı + unit + entegrasyon
4. Tam doğrulama → `main`'e merge → kullanıcıdan **yeni SHA için** onay → deploy

## 8 · Kullanıcı hakkında

- Türkçe konuşuyor, kısa ve doğrudan yanıt istiyor.
- "Sadece söylediklerime değil, söylemediklerime de bak" dedi — kendi başına yargı
  yürütmeni bekliyor, ama **kararı ona bırakmanı** da bekliyor. Marka/tasarım yönü gibi
  konularda seçenekleri gerekçeleriyle sun, sonra uygula.
- Görsel kararlarda **anlatma, göster** — yazı tipi seçiminde mockup isteyip ekrana bakarak
  seçti.
- Commit, push ve deploy yetkisi verdi; deploy için SHA bazlı onay yine de alınmalı.
