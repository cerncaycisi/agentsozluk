# Devam notu — 2026-08-22, oturum duraklatıldı

Önceki not: `docs/DEVAM_2026-08-21.md`. Bu onun üstüne yazıyor.

## Durum

Canlı = main = `1c8bb61`. Toplum **çalışıyor** (ayar sürümü 210). Açık PR yok.
Bugün **19 PR** ve **dört deploy turu**, dördü de ilk denemede geçti.

## Canlıda olan yeni davranış

- Yazar **yazmadan önce başlığı okuyor**: `trendingTopics` ve `followedTopics`
  entry önizlemesi taşıyor.
- **Gündem, Yeni başlıklar, takip edilen başlıklar ve takip edilen yazarların
  işi** perception'da. Sitedeki dört akıştan üçü artık ajanda.
- Tekrar kapıları: başka yazarları görüyor, `tek başına göstermiyor` kapanışını
  yakalıyor, yazarın kendi kavram tekrarını yakalıyor.
- Bağlam bütçesi 64 → **160 KB** (ölçüm sınırın bağlayıcı olduğunu gösterdi).
- Alıntı işaretçisi `yazarken`'i atıf sanmıyor; persona düzenlemesinde CAS var.
- `RUNTIME-004` kapandı → geliştirme kapısı **465 PASS / 1 BLOCKED**.

## Sıradaki iş — önem sırasıyla

### 1. Aramadan başlık açma (Gökhan'ın açık şikâyeti, AJAN KOŞUYOR)

Gökhan: _"lan ben niye hala arayarak başlık açamıyorum?"_ ve
_"hangi sözlükte 'başlık aç' diye bi sayfa var? iki sözlük benchmarklattım sana."_

İkisi de haklı. `docs/BENCHMARK_GIRISLI_2026-08-20.md` §5: Normal Sözlük'te
olmayan başlık arandığında `/baslik/<başlık>` açılıyor — **ayrı "başlık aç"
sayfası yok**, başlık ilk entry'yle doğuyor. Bizde `/baslik/ac` diye ayrı sayfa
var ve `/ara` hiç çıkış sunmuyor.

**Üç hatam:** (a) `/ara`'nın çıkmaz olduğunu tespit edip sıraya almadım;
(b) E2E yeşildi ama **yanlış yüzeyi** ölçüyor (autocomplete, arama sayfası değil);
(c) benchmark'ı ben yaptırdım, okumadan `/baslik/ac?title=` linki ekliyordum —
yanlış modeli pekiştiriyordu, attım.

**Ajanın işi yarıda kaldı** (bilgisayar uykuya geçti), ama iş büyük ölçüde
bitmiş görünüyor — 23 dosya, +568/−353. Yedeklendi:

- `scratchpad/baslikac-yarim.patch` (izlenen dosyalar)
- `scratchpad/baslikac-yeni/` (yeni dosyalar)

Yaptığı belli olanlar: `unopened-topic-view.tsx` (açılmamış başlık görünümü),
`modules/topics/domain/proposal.ts` (+ iki test), `/baslik/ac/page.tsx` ve
`prefill-topic-title.tsx` **silinmiş** — yani ayrı sayfayı kaldırma yolunu
seçmiş. Dört E2E dosyası ve `public-urls` güncellenmiş.

**Devam ederken:** ajanın kendi raporu alınamadı, o yüzden kararlarının
gerekçesini koddan okumak gerekiyor. Özellikle `/baslik/ac`'nin silinmesi ve
`topic-canonical-suggestions.tsx`'in kaldırılması gerekçesiz duruyor — ikisi de
geri dönüşü olan ama gözden geçirilmesi gereken kararlar. Testler koşulmadı.

### 2. Ses: soru düzeltmesi İŞE YARAMIYOR — gerçek sebep bulundu

Bugün gönderdiğim `writing-variation` v6 (soru izni) **ölçümle elendi**: 99 codex
çağrısı, eşleştirilmiş tasarım. Aynı hücrede eski satır 2/16 soru üretti, yeni
satır 0/16. v6'nın kendi hipotezi **p=0,002** ile reddedildi.

**Gerçek sebep: öz-çıpalama.** Aynı `runId`, aynı persona, aynı başlık, aynı
MICRO talimatı; tek değişken `ownRecentEntries`:

- kendi geçmişi **gizliyken**: 8 entry'nin **6'sı** ≤10 kelime, medyan 10
- kendi son 8 entry'si **görünürken**: **1'i**, medyan 14 (p=0,041)
- ikinci hâl canlı üretimden **istatistiksel olarak ayırt edilemiyor** (p=0,19)

Yani yazar form talimatından çok **kendi görünür geçmişini taklit ediyor**. Her
uyanışta sekiz tane 25 kelimelik bildirim paragrafı görüyor, bant kendini
besliyor. Kısa kuyruk model sınırı değil, **bağlam sınırı**.

Sonraki paket `ownRecentEntries` sunumunu hedeflemeli — kota ekleyerek değil.
Uzun kuyruk ayrı sorun: harness'ta bile 100 kelime aşılmıyor (0/8, max 60).

Ayrıntı: ajan raporu ve `scratchpad/m5/` (99 prompt, 96 çıktı, kör yargıç).

### 3. D-8 tanım yuvası kapısı — hazır, indirilmedi

Ajan `TOPIC_DEFINITION_REPEATED` yazdı: aynı başlıkta **tanım yuvasını ikinci kez
açmayı** yakalıyor. Korpus: 42 red (%2,8), **36 gerçek / 6 tartışmalı** (%86
isabet), ha-leylim üçlüsünün 2. ve 3. entry'si yakalanıyor, meşru dönüşler
(yeni örnek, karşı görüş, sonucu değiştiren gelişme) geçiyor.

Yaklaşım gerekçesi önemli: sözlüksel örtüşme ekseni **kapalı** — IDF denemesi işi
kötüleştirdi (aday 287 içinde 127. sırada). Yapısal işaret (tanım açılışı)
kelime seçiminden bağımsız.

Worktree: `scratchpad/wt-d8b`. **Commit edilmedi.**

### 4. M2 — tek bloker kaldı

`DONE-082`, o da Gate 10'un 7 günlük penceresine bağlı. Gökhan'ın kararı
(21 Ağu): **göndermeye devam, ölçümü sonraya bırak.** Her davranış release'i
pencereyi sıfırlıyor; bugün dört release oldu.

### 5. Kapasite (M-4 / O2)

**"3 saat" diye bir şey yok** — o `AGENT_RUNTIME_BENCHMARK_TIMEOUT_MS × 10 × 2`,
yani her senaryo timeout'a çarparsa. Gerçek CLI süresi bugünkü koşulardan
**~80–100 dakika** (ajanın Temmuz verisinden çıkardığı 17–31 dk bugün geçerli
değil; koşular yavaşladı).

Üç spec bulgusu: (a) benchmark fixture'ı production perception'ın **%1,6–8,2**'sini
dolduruyor, 22 anahtarın 16'sı boş; (b) n=10'da p75 için %95 güven aralığı
kurulamıyor ve p95 ≡ max; (c) kabul kapısı harness çıktısını elle yazılmış
JSON'dan ayırt edemiyor.

**M-4 belki benchmark istemiyor:** benchmark yazar sayısından bağımsız, hep aynı
10 senaryo. 14→36 geçişi yeni `plannedContentRuns` istiyor, yeni ölçüm değil.

### 6. Kalan tahta

D-10 (Madde 36 kapısı, düşük öncelik), M-3 (pencere), M-5'in kalan iki maddesi:
kapasite tazeliği ve **ilişki evrimi ölü** (19 gündür sıfır
`UPDATE_RELATIONSHIP_NOTE`, sıfır yeni `FOLLOW_USER`).

## Ortam notları

- `/private/tmp/agent-sozluk-known_hosts` **gece temizlendi**. Yeniden kurarken
  körlemesine kabul etme: `ssh-keyscan` → parmak izini
  `SHA256:BVirvnH5qPzzK18ZGLhO90LObtFze38qicLybEwQ5fI` ile karşılaştır.
- Toplumu panelden başlatırken **koordinatla tıklama**; `find` ile `ref` al.
  Koordinat tıklaması bugün üç kez sessizce geçmedi (React kontrollü girdi).
- `auth-content.spec.ts` `net::ERR_ABORTED` flake'i var; yeniden koşuda geçiyor.

## Bugün tekrarladığım hatalar

1. **`git checkout --` ile kendi işimi sildim** (ikinci kez). Mutasyon testinden
   sonra geri alırken yedek kullan.
2. **Yanlış yüzeyi doğrulayan yeşil teste güvendim.**
3. **Kendi yaptırdığım benchmark'ı okumadan iş yaptım.**

Ajanlar bugün **sekiz teşhisimi** düzeltti ve hepsinde haklıydılar. Bağımsız
inceleme dört canlı hata buldu. Tempoyu korumak istiyorsan bunları kes.
