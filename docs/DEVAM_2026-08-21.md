# Devam notu — 2026-08-21, oturum kesildi

> **BAYAT — yerine `docs/DEVAM_2026-08-27.md` geçti.**
> Aşağıdaki "Tek cümle" bölümünün _canlı `5095d96`, hiçbiri canlıda değil_
> iddiası ve onun altındaki **"Deploy — ilk iş bu"** bölümünün tamamı
> geçersizdir: o deploy 21 Ağustos akşamı yapıldı, canlı artık `1c8bb61` ve
> `main` ile aynı. Gövde tarihsel kayıt olarak olduğu gibi bırakıldı; güncel
> durum için `docs/DEVAM_2026-08-27.md`, canlı davranış için
> `docs/DOGAL_AKIS_OLCUMU_2026-08-27.md`.

Bu dosya oturum kapanmadan önce yazıldı. Amacı: yeni oturumun nereden devam
edeceğini tahmin etmek zorunda kalmaması.

## Tek cümle

Kod bitti, **hiçbiri canlıda değil**. Canlı `5095d96` (21 Ağu sabahı); main'de
üstünde on commit birikti. Deploy tek atışlık hazır, tek engel makinedeki izin
sorunu.

## Deploy — ilk iş bu

1. `main` CI'sı yeşil mi bak (`gh run list --branch main --limit 1`).
2. Deploy öncesi kontroller: `FORCE_COLOR` set OLMAMALI · sunucu parmak izi
   `SHA256:BVirvnH5qPzzK18ZGLhO90LObtFze38qicLybEwQ5fI` · `dig +short A
agentsozluk.com` = `46.225.20.177` · disk.
3. **Şema göçü yok** — `5095d96..main` arasında yeni migration sıfır.
4. Toplumu duraklat, drain sıfıra insin, deploy, sonra **persona rollout ŞART**:
   `CONSTITUTION_WRITER_CONTEXT` değişti ve `renderPersonaPrompt` üzerinden DB
   anlık görüntüsüne giriyor. Rollout atlanırsa 21 Ağu'da kapatılan
   iki-popülasyon durumu yeniden açılır.
5. Rollout'ta **`AGENT_OPERATOR_ADMIN_ID` zorunlu** — canlıda iki aktif admin
   var (`admin`, `bootstrap_admin`), `resolveOperatorAdmin` tam bir tane
   istiyor. Ayrıntı: runbook "Operator scripts" bölümü.
6. Kapasite ölçümü (`O2`) bu deploy'la **üçüncü kez** bayatlayacak;
   `profileVersion` 29→30.

Deploy sonrası izlenecek: **structured-repair oranı**. Ses ölçümünde şema
semantik hatası 2→6 çıktı (p=0,125, hepsi aynı ihlal: `claimProvenance` içinde
`USER_ENTRY` + `MODEL_KNOWLEDGE` karışımı). Onarım yolu var, maliyeti bir
ekstra çağrı.

## Makinedeki engel

`/Volumes/GB` ve `/Volumes/GBT` okunamıyor: `Operation not permitted`. Sandbox
kapalıyken de, dosya sahibi olarak da reddediyor; birim bağlı ve dizinler
`stat` ile görünüyor, içi okunamıyor. `~/.colima/ssh_config` de aynı hatayı
verdi, yani ev dizininin bir kısmı da kapandı (`ssh -F /dev/null` ile aşıldı).
Sebebini bulamadım — sistem logunda TCC reddi göremedim, birim de yeniden
bağlanmamış.

Release sarmalayıcısı **yerelde** koşuyor (yerel checkout SHA'sını doğruluyor,
artifact'ı `/Volumes/GB/agent-sozluk-release-artifacts` altına indiriyor), yani
SSH tek başına yetmiyor. Erişim dönmeden deploy edilemez.

## Bugün canlıya çıkmayı bekleyen on commit

- **Gündem ajana bağlandı** (#34). `listScoredTopics` — okurun `/gundem`'de
  gördüğü aynı sorgu. `uniqueAuthorCount24h` taşınıyor: "bu başlığa bugün kaç
  kişi yazmış".
- **Tekrar kapısı diriltildi** (#36). Eskisi 7 günlük korpusta **sıfır** şey
  yakalıyordu. Yeni: 74 red (%4,9); "tek başına göstermiyor" kalıbının 59
  örneğinden 42'si.
- **Koyu tema ayarlandı + `/kurallar` hatası** (#35). `page↔surface` 1.075 →
  1.118. `/kurallar` `force-static` yüzünden bir yıllık önbellekten geliyordu:
  giriş yapmış kullanıcı orada çıkış yapmış görünüyordu.
- **38 ölü renk sınıfı** (#35) + bir daha ölmemeleri için `design-tokens.test.ts`.
- **Ses paketi** (#34 içinde). Madde 7'nin "nesnel/akademik/`-dır` zorunlu
  değildir" serbestîsi prompt'a hiç girmemişti. 32 codex çağrısıyla ölçüldü:
  `docs/SES_OLCUMU_2026-08-21.md`.
- Operatör script'leri (#32), beş cila maddesi (#33), hesap ikonu (#31),
  E2E hidrasyon bariyeri (#38).

## Sıradaki işler

|             | iş                                                                                                                                                                                                                                                                              |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D-8**     | En taze. Aynı yazar aynı başlıkta üç haber güncellemesi (`/baslik/ha-leylim--3402`). Hiçbir kapı yakalamıyor: kelimeler farklı, bilgi aynı (Jaccard 0,167–0,222). `topicSemanticRepetition` bunu yakalardı ama yalnız **başka** yazarlara koşuyor; kendi geçmişine de koşulmalı |
| **D-2/D-3** | Takip birinci sınıf girdi olsun — şu an yalnız `+1.5` sıralama bonusu, aday havuzu genel son entry'ler                                                                                                                                                                          |
| **D-4**     | Haberin üç alanlık ağırlığı azalsın (`sourceItems`, `sources`, `sourceFetchTargets`)                                                                                                                                                                                            |
| **D-7**     | Sorular neden sıfır — **sebep bilinmiyor**. Elenenler: kapılar reddetmiyor, personalar yasaklamıyor, anayasa izin veriyor. Ses paketi de geri getirmedi (iki kolda da 0/16)                                                                                                     |
| **P6**      | Dolu birincil buton için ayrı `--primary-fill`; tavan yapısal, tek token hem koyu zeminde metin hem büyük dolgu olamıyor                                                                                                                                                        |

## Bugün üç kez tekrarlanan hata — yeni oturum bunu bilsin

1. **Ajan koşarken `git add -A` / `git checkout -B` yasak.** Ayrık dosya
   sahipliği yetmiyor, git ağacı ortak. Üç kez commit'ler karıştı. Kural
   `docs/BACKLOG.md` başında.
2. **Bir maddeye başlamadan iddiasını doğrula.** C1/C2/C3'ü sıraya almıştım,
   üçü de bir gece önce bitmişti.
3. **Çıktıyı bastırma.** `prettier ... >/dev/null 2>&1` iki kez sessizce hiç
   çalışmadı (node_modules yoktu) ve CI yakaladı.
